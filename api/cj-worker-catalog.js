const { serverConfig, serverHeaders } = require('./_lib/supabase-server');
const { ensureAccessToken, CJ_BASE } = require('./_lib/cj');
const { convertToIls } = require('./_lib/shipping');
const { requireWorker } = require('./_lib/cj-worker-auth');

const clean=(v,n=300)=>String(v??'').trim().slice(0,n);
const sleep=(ms)=>new Promise(r=>setTimeout(r,ms));
let lastCj=0;
async function cj(path,{method='GET',body}={}){const wait=Math.max(0,1150-(Date.now()-lastCj));if(wait)await sleep(wait);lastCj=Date.now();const token=await ensureAccessToken();const headers={Accept:'application/json','CJ-Access-Token':token};if(body!==undefined)headers['Content-Type']='application/json';const r=await fetch(`${CJ_BASE}${path}`,{method,headers,body:body===undefined?undefined:JSON.stringify(body)});const raw=await r.text();let j={};try{j=raw?JSON.parse(raw):{}}catch{j={message:raw.slice(0,250)}};const code=Number(j?.code);if(!r.ok||j?.result===false||j?.success===false||(Number.isFinite(code)&&![0,200].includes(code)))throw new Error(`cj_${j?.code||r.status}_${clean(j?.message||'failed',180)}`);return j}
async function dbGet(path){const{supabaseUrl}=serverConfig();const r=await fetch(`${supabaseUrl}/rest/v1/${path}`,{headers:serverHeaders()});if(!r.ok)throw new Error(`db_get_${r.status}`);return r.json()}
async function patch(table,filter,data){const{supabaseUrl}=serverConfig();const r=await fetch(`${supabaseUrl}/rest/v1/${table}?${filter}`,{method:'PATCH',headers:serverHeaders({'Content-Type':'application/json',Prefer:'return=representation'}),body:JSON.stringify({...data,updated_at:new Date().toISOString()})});if(!r.ok)throw new Error(`${table}_patch_${r.status}_${(await r.text()).slice(0,160)}`);return(await r.json())[0]||null}
function invRows(d){return Array.isArray(d?.inventories)?d.inventories:[]}
function invTotal(d){return invRows(d).reduce((s,x)=>s+(Number(x?.totalInventory??x?.totalInventoryNum)||0),0)}
function origin(d){const rows=invRows(d),cn=rows.find(x=>String(x?.countryCode||'').toUpperCase()==='CN'&&(Number(x?.totalInventory??x?.totalInventoryNum)||0)>0);return clean(cn?.countryCode||rows.find(x=>(Number(x?.totalInventory??x?.totalInventoryNum)||0)>0)?.countryCode||'CN',2).toUpperCase()}
function freight(rows,preferred){const valid=(Array.isArray(rows)?rows:[]).filter(x=>Number.isFinite(Number(x?.logisticPrice))&&Number(x.logisticPrice)>=0);if(preferred){const hit=valid.find(x=>clean(x?.logisticName,120).toLowerCase()===clean(preferred,120).toLowerCase());if(hit)return hit}return valid.sort((a,b)=>Number(a.logisticPrice)-Number(b.logisticPrice))[0]||null}
function retail(target,current){const c=Number(current||0);return Number.isFinite(c)&&c>=target?Number(c.toFixed(2)):Number((Math.ceil(target*10)/10).toFixed(2))}
async function syncProduct(p,minProfit,job=null){
  const vid=clean(p.fulfillment_variant_id||job?.provider_variant_id,160),sku=clean(p.fulfillment_sku||job?.provider_variant_sku,180),pid=clean(p.fulfillment_product_id||job?.provider_product_id,180);if(!vid||!sku||!pid)throw new Error('cj_identity_incomplete');
  const detail=(await cj(`/product/variant/queryByVid?vid=${encodeURIComponent(vid)}&features=enable_inventory`)).data||{};const from=origin(detail),stock=invTotal(detail);const options=(await cj('/logistic/freightCalculate',{method:'POST',body:{startCountryCode:from,endCountryCode:'IL',products:[{quantity:1,vid}]}})).data||[];const selected=freight(options,p.fulfillment_logistic_name||job?.provider_snapshot?.selectedFreight?.logisticName);if(!selected)throw new Error('cj_shipping_to_il_unavailable');
  const priceUsd=Number(detail.variantSellPrice),shipUsd=Number(selected.logisticPrice);if(!Number.isFinite(priceUsd)||priceUsd<0)throw new Error('cj_price_missing');const priceIls=await convertToIls(priceUsd,'USD'),shippingIls=await convertToIls(shipUsd,'USD'),sellingPrice=retail(priceIls+Number(minProfit||25),p.selling_price),now=new Date().toISOString(),totalCost=Number((priceIls+shippingIls).toFixed(2));
  const snap={source:job?'cj_sourcing':'cj_sync',cjProductId:pid,cjVariantId:vid,cjVariantSku:sku,variantSellPriceUsd:priceUsd,inventory:stock,origin:from,freight:selected,priceIls,shippingIls,checkedAt:now};
  await patch('products',`id=eq.${encodeURIComponent(p.id)}`,{supplier:'cj',supplier_id:`cj:${pid}`,supplier_product_id:pid,supplier_sku_id:vid,supplier_price:priceUsd,supplier_currency:'USD',supplier_price_ils:priceIls,supplier_shipping:shippingIls,shipping_currency:'ILS',supplier_in_stock:stock>0,supplier_stock:stock,supplier_shipping_available:true,supplier_ship_from_country:from,supplier_sync_error:null,shipping_sync_error:null,last_sync_at:now,shipping_last_checked_at:now,selling_price:sellingPrice,auto_fulfill_max_cost:Math.max(Number(p.auto_fulfill_max_cost||0),Number((totalCost*1.10).toFixed(2))),fulfillment_provider:'cj',fulfillment_product_id:pid,fulfillment_variant_id:vid,fulfillment_sku:sku,fulfillment_origin_country:from,fulfillment_logistic_name:clean(selected.logisticName,120),fulfillment_provider_status:job?'verified_sourcing':'verified_sync',fulfillment_provider_snapshot:snap,fulfillment_verified_at:now,fulfillment_ready:false});
  const ready=stock>0&&(sellingPrice-priceIls)>=Number(minProfit||25);if(ready)await patch('products',`id=eq.${encodeURIComponent(p.id)}`,{fulfillment_ready:true});
  if(job)await patch('product_intake_jobs',`id=eq.${job.id}`,{status:ready?'published':'needs_profit_rule',provider_status:ready?'sourcing_verified':'needs_profit_rule',provider_product_id:pid,provider_variant_id:vid,provider_variant_sku:sku,provider_snapshot:snap,store_product_id:p.id,last_error:null,processed_at:now});
  return{id:p.id,ready,stock,priceIls,shippingIls,sellingPrice,service:selected.logisticName};
}
module.exports=async function handler(req,res){res.setHeader('Content-Type','application/json; charset=utf-8');res.setHeader('Cache-Control','no-store');try{if(!await requireWorker(req,res))return;const settings=(await dbGet('site_settings?id=eq.primary&select=minimum_profit_ils&limit=1'))[0]||{},minProfit=Number(settings.minimum_profit_ils||25),products=await dbGet('products?select=*&active=eq.true'),byId=new Map(products.map(p=>[String(p.id),p])),jobs=await dbGet('product_intake_jobs?select=*&provider_status=eq.cj_quote_ready'),results=[],synced=new Set();
for(const job of jobs){const p=byId.get(String(job.store_product_id||''));if(!p)continue;try{results.push({kind:'finalize',...(await syncProduct(p,minProfit,job))});synced.add(String(p.id))}catch(e){results.push({kind:'finalize',id:p.id,error:clean(e.message,220)})}}
for(const p of products.filter(x=>String(x.fulfillment_provider||'').toLowerCase()==='cj'&&x.fulfillment_product_id&&x.fulfillment_variant_id&&x.fulfillment_sku&&!synced.has(String(x.id)))){try{results.push({kind:'refresh',...(await syncProduct(p,minProfit,null))})}catch(e){await patch('products',`id=eq.${encodeURIComponent(p.id)}`,{fulfillment_ready:false,supplier_sync_error:clean(e.message,300)}).catch(()=>{});results.push({kind:'refresh',id:p.id,error:clean(e.message,220)})}}
return res.status(200).json({ok:true,minProfit,ready:results.filter(x=>x.ready).length,errors:results.filter(x=>x.error).length,results})}catch(e){console.error('CJ catalog worker:',e.message);return res.status(500).json({ok:false,error:clean(e.message,240)})}};
